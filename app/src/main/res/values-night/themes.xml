<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base dark application theme. -->
    <style name="Base.Theme.Camping" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/nunito_font_family</item>
        <item name="fontFamily">@font/nunito_font_family</item>
        <item name="colorPrimary">@color/primary_dark</item> <!-- Dark theme primary color -->
        <item name="colorAccent">@color/accent_dark</item> <!-- Dark theme accent color -->
        <item name="android:background">?android:attr/colorBackground</item> <!-- Use system background color -->
        <item name="android:textColor">?android:attr/textColor</item> <!-- Use system text color -->
    </style>

    <style name="Theme.Camping" parent="Base.Theme.Camping" />
</resources>